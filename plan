request validation.
check and guard cross-thread accesses to common structures.
signal handling.

viewer:
    sdl.

--
game entities (shell).
game entities behaviour (shell).
Port existing code (shell).
