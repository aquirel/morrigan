test client.
    client api.

game entities (shell).
game entities behaviour (shell).
Port existing code (shell).
shell viewer notifications
move all to single thread?

put isnormal() everywhere.
cos and sin <= 1.

